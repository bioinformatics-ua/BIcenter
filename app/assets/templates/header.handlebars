<ul class="nav navbar-nav" >

</ul>

<div class="navbar-custom-menu">
    <ul class="nav navbar-nav">
        <li class="dropdown notifications-menu">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <i class="fa fa-bell-o"></i>
                <span view-element="notification_counter" class="label label-warning">0</span>
            </a>
            <ul class="dropdown-menu">
                <li>
                    <!-- inner menu: contains the actual data -->
                    <ul view-element="notification_list" class="menu">

                    </ul>
                </li>
            </ul>
        </li>
        <li class="dropdown user user-menu">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                <i class="fa fa-user fa-fw"></i>
                <span class="hidden-xs">{{name}}</span>
            </a>
            <ul class="dropdown-menu">
                <!-- User image -->
                <!--<li class="user-header">-->
                    <!--<p>{{name}}<small>{{email}}</small></p>-->
                <!--</li>-->
                <li class="user-header">
                    <img src="https://www.gravatar.com/avatar/{{#md5 email}} {{/md5}}"/>
                    <p>{{name}}<small>{{email}}</small></p>
                </li>
                <!-- Menu Footer-->
                <li class="user-footer">
                    <!--<div class="pull-left">-->
                    <!--<a href="#" class="btn btn-default btn-flat">Profile</a>-->
                    <!--</div>-->
                    <div class="pull-right">
                        <a href="#" view-element="logoutBtn" class="btn btn-default btn-flat">Sign out</a>
                    </div>
                </li>
            </ul>
        </li>
    </ul>
</div>